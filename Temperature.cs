using System;

namespace _22FIFA
{
    class Temperature
    {
        public Temperature(int temperature)
        {
            Console.WriteLine(" TEMPERATURE EXTERIEURE : " + temperature + " °C"); // Affiche la température prévue au coup d'envoi du match
            Console.ReadLine();
        }
    }
}
