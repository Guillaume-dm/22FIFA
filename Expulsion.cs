using System;

namespace _22FIFA
{
    class Expulsion
    {
        public Expulsion(string name_red, string Equipe)
        {
            Console.WriteLine("CARTON ROUGE - " + Equipe);
            Console.WriteLine("  " + name_red);
        }
    }
}
